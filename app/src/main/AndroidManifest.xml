<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hwk2.cis350.upenn.edu.wolfofwharton">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpScreenActivity" />
        <activity android:name=".TickerSearchActivity" />
        <activity android:name=".StockInfoScreenActivity" />
        <activity
            android:name=".PortfolioActivity"
            android:label=""
            android:theme="@style/AppTheme" />
        <activity android:name=".WatchListActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".TransactionHistoryActivity" />
        <activity android:name=".InstructionsActivity"></activity>
    </application>

</manifest>